There was an error with the Authorization Header, check if you are using bearer correctly and the token exists.
If the problem is related with the token itself you can create another one using this POST http://localhost:8080/api/users/token api call.

Hope it helps :)